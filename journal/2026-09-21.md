---
date: 2026-09-21
---

- poco discharging on the dock: not the dock, not renegotiation. 5 m aftermarket usb-a cable: fine alone, 437 mA draining (status said CHARGING) once hubs + ssd hang off it. c-to-c: -2.7 A. `termux-battery-status` current sign: negative = charging
- monitoring: prometheus + alertmanager + blackbox + node_exporter on the poco, repo `sandravwc/monitoring`, ntfy topic, deadman timer on the workstation
  - rule from today: servers on the poco, nothing phone is a pure client (almost installed the monitor there)
  - android walls, each with a side door: /proc/stat & loadavg denied -> sysinfo() + cpuidle sysfs; node_exporter >=1.9 SIGSYS -> 1.8.2; go can't resolve or find CAs -> tinyproxy + SSL_CERT_FILE; alertmanager gossip -> off
  - lesson: `( bin & )` from ssh for a quick test = orphan holding the port, an hour of "why no notification". kill the test, then start the service
  - [[android/prometheus-termux]]
- monitoring web: prom./alerts. behind haproxy basic auth, grafana. with own login + provisioned "poco" dashboard. cnames added
  - detour: termux haproxy has no crypt(3) -> tried prometheus' own bcrypt auth -> every loopback client needed the password (self-scrape, am push, grafana). auth at the edge, plaintext userlist in ~/haproxy.d/05-auth.cfg outside the repo
  - readmes trimmed across repos: intro = tech only, no "why on the poco" prose
  - [[android/prometheus-termux]], [[android/haproxy-termux]]
- nfs stale during a move through the mount, gnome + shell hung, reboot. rclone handles are path-based, rename = ESTALE for the tree, restart doesn't help. mount now `soft`, move on the poco instead. [[android/nfs-export]]
- nfs stale during a move through the mount, gnome + shell hung, reboot. rclone handles are path-based, rename = ESTALE for the tree, restart doesn't help. mount now `soft`, move on the poco instead. [[android/nfs-export]]
- nfs "not responding" (not stale) while shoko hashed the library: android fuse saturated, rclone starved. librhash was missing too (C# fallback), rhash-glibc symlinked in. [[android/nfs-export]] [[android/shoko-termux]]
- grafana dashboard reworked: rows per service/host area, UP/DOWN probe tiles, cert thresholds (yellow <30 = renew missed, red <3), haproxy_exporter instead of the textfile hack, storage per mount with %. dashboards split into `sandravwc/grafana-dashboards`. [[android/prometheus-termux]]
- ble.sh on the workstation was arch `blesh` 0.3.4 (2019): history only written on clean exit, tab-complete backslash-escapes everything. swapped to `blesh-git` (0.4.0-devel4): `complete_requote_threshold=0` default = `'...'` requote, `bleopt history_share=1` = `history -a` per command + shared across sessions. added to all blerc snippets (comfy scripts, termux-setup)
  - rsync/scp still `'foo\ bar'` after the upgrade: bash-completion escapes scp/rsync local paths itself (`_comp_cmd_scp__escape_path`), ble.sh quotes the result again. blerc hook via `blehook complete_load` + `ble/function#advice after` on the completer, dequote COMPREPLY with `ble/syntax:bash/simple-word/eval`. fzf's `_fzf_path_completion` owns `rsync` and calls the original inside -> guard on `$COMP_LINE:$COMP_POINT`. same snippet in all blerc heredocs
  - ambiguous `<Tab>` (common prefix, many candidates) still inserted backslash-escaped: `complete_requote_threshold` only touches the final unique insert. advice after `ble/complete/candidates/determine-common-prefix`, requote `ret` as open `'...`, next tab continues inside the quote
  - two more holes: tab-tab menu cycling inserts the raw escaped candidate (`menu-complete.class/onselect`), and `requote-final-insert` compares quoted vs escaped length so one special char (`Vinland\ Saga` 13 vs `'Vinland Saga'` 14) never requotes. advice after onselect reusing `requote-final-insert`, and the threshold arithmetic patched via `declare -f` + eval to discount the two quotes. common-prefix hook moved to `before ble/complete/insert` (rewriting `ret` in determine-common-prefix broke the `$COMPS` prefix check -> nothing inserted). advice args: `ADVICE_WORDS[0]` is the function name, `$1` too
