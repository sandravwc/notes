---
date: 2026-09-21
---

- poco discharging on the dock: not the dock, not renegotiation. 5 m aftermarket usb-a cable: fine alone, 437 mA draining (status said CHARGING) once hubs + ssd hang off it. c-to-c: -2.7 A. `termux-battery-status` current sign: negative = charging
- monitoring: prometheus + alertmanager + blackbox + node_exporter on the poco, repo `sandravwc/monitoring`, ntfy topic, deadman timer on the workstation
  - rule from today: servers on the poco, nothing phone is a pure client (almost installed the monitor there)
  - android walls, each with a side door: /proc/stat & loadavg denied -> sysinfo() + cpuidle sysfs; node_exporter >=1.9 SIGSYS -> 1.8.2; go can't resolve or find CAs -> tinyproxy + SSL_CERT_FILE; alertmanager gossip -> off
  - lesson: `( bin & )` from ssh for a quick test = orphan holding the port, an hour of "why no notification". kill the test, then start the service
  - [[android/prometheus-termux]]
- monitoring web: prom./alerts. behind haproxy basic auth, grafana. with own login + provisioned "poco" dashboard. cnames added
  - detour: termux haproxy has no crypt(3) -> tried prometheus' own bcrypt auth -> every loopback client needed the password (self-scrape, am push, grafana). auth at the edge, plaintext userlist in ~/haproxy.d/05-auth.cfg outside the repo
  - readmes trimmed across repos: intro = tech only, no "why on the poco" prose
  - [[android/prometheus-termux]], [[android/haproxy-termux]]
- nfs stale during a move through the mount, gnome + shell hung, reboot. rclone handles are path-based, rename = ESTALE for the tree, restart doesn't help. mount now `soft`, move on the poco instead. [[android/nfs-export]]
