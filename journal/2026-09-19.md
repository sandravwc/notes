---
date: 2026-09-19
---

- Claude Code session: edited 2 file(s): /home/mrk/.claude/hooks/logseq-stop-log.sh,/home/mrk/.claude/settings.json
- migrated notes graph: logseq (db version) -> foam vault, git init'd, no remote yet
  - 66 pages + 4 journals converted via `logseq` CLI export, 47 built-in schema pages dropped
  - review: [[MIGRATION-REVIEW]]
  - stripped zero-width-space bug from old code blocks (broke copy-pasted shell scripts)
- replaced logseq-cli skill with foam skill, hook renamed -> foam-stop-log.sh, repointed at journal/
- fixed duplicate "cheat sheet" (and 6 other) graph nodes: dropped `tags: [category]` frontmatter that just repeated the directory name -- Foam renders tags as their own node, collided with the hub note
- fixed 699 markdownlint issues, added `.markdownlint.json` disabling rules that fought real conventions (long command lines, `<placeholder>` syntax, Logseq `### label` bullets)
- fixed false orphans: `android.md`/`kafka.md` hub pages were missing (Logseq draws namespace edges from naming alone, Foam doesn't) -- added them, plus restored a dropped `[[cheat sheet/ssh-mesh]]` link
- registered official `foam mcp` (foam-cli) as a project-scoped, read-only MCP server for this vault -- pending one-time approval
- mealprep dyndns: `server/dyndns.py` cron */5 on the poco, public ip via router upnp (enabled today on the tp-link) else ifconfig.co, rewrites A record via autodns bulk task 0202001, creds reused from `~/.acme.sh/account.conf`
  - `poco.xn--bdk.dog` now on the public ip, nat loopback from inside works (curl :8443 -> 200)
  - todo: confirm 8443 forward from outside (mobile data)
  - lesson: ask before touching router/account settings
  - notes: [[android/mealprep-server]]
- mealprep public: router "Virtual Servers" (tp-link's name for dnat) tcp 8443 -> poco, loopback ok, reachable from outside
  - auth: `server/auth.py`, password form + 1y cookie, ntfy click links carry `?t=<AUTH_TOKEN>` and set the cookie, logout in ☰, in-app ban 5 fails/h per ip
  - `AUTH DENY|FAIL|BANNED <ip>` lines in the sv log, fail2ban-shaped for the load balancer later
  - lan http :8090 stays open, never forwarded
  - verified from phone: push link logs in, cookie sticks, logout works
- mealprep lb chain: haproxy :8443 tls -> anubis :8923 -> app :8090, configs versioned in repo `deploy/`
  - anubis static arm64 tarball runs rootless in termux as-is, no build. browser ua -> pow challenge, GPTBot -> deny, curl -> passes to app login
  - haproxy from `pkg`, 3.4. `option forwardfor` + X-Real-IP, app auth lines now carry real ips
  - app.py dropped own tls, trusts X-Forwarded-Proto for "public" vs lan
  - acme.sh reloadcmd -> `deploy/install-cert.sh` bundles chain+key for haproxy
  - pages: [[android/haproxy-termux]], [[android/anubis-termux]]
- shoko: "no permission" on the ssd inside proot = each `proot-distro login` needs its own `--bind`, shoko/sshd services had none, `--mount` isn't a flag. fixed, then made it moot:
- shoko native in termux via glibc-runner, proot debian removed (13G freed). repo `sandravwc/shoko-termux`
  - bionic RID considered, blocked by Magick.Native (imagemagick static, glibc only). glibc-runner = same binaries, patched interpreter, no ptrace
  - LD_PRELOAD shim for getifaddrs/if_nametoindex over ioctl: android denies netlink bind + SIOCGIFINDEX on AF_UNIX, .net then saw 0 ifaces, shoko silently offline. strace found it, proot had hidden it (emulates netlink)
  - .net sdk under glibc-runner dead (named mutex wants /tmp). runtime fine
  - lessons: `pkill -f <x>` from ssh kills the ssh session (twice today). glibc .so in LD_PRELOAD breaks every bionic command after it
  - pages: [[android/glibc-runner]], [[android/shoko-termux]]
- haproxy split: `~/haproxy.d/` dir config, `00-base.cfg` + one backend file per app repo, backend picked by host label. anubis per app (`anubis`, `anubis-shoko`)
  - wildcard cert `*.poco.<zone>`, `shoko.`/`mealprep.` as CNAME -> poco. `acme.sh --issue` alone doesn't reinstall, `--install-cert` after
  - [[android/haproxy-termux]]
- git: never commit on the phone clone, it was a commit behind and the push bounced. edit in `~/workdir/git/<repo>`, push, `git pull` on the phone
- ssh host `poco_f5_pro-proot-debian` dropped (config + termux-setup repo)
