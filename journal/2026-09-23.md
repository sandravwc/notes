---
date: 2026-09-23
---

- comfy scripts moved out of the vault into [comfy-env](https://github.com/sandravwc/comfy-env), termux-setup merged in as `termux/` (history kept via subtree)
  - one self-contained script per target (el, el-zfs, el-k8s, el7, proxmox, termux), heredocs edited in place, no install args
  - pages [[cheat sheet/enterprise linux/comfy script]], [[hypervisors/proxmox/comfy script]], [[android/termux-setup]] now just link + curl line
- screenrc: `bindkey "^[^M" stuff "^J"` -- alt+enter = newline in claude inside screen (screen eats the kitty/csi-u key protocol, ESC CR otherwise submits)
- mihon sync options: syncthing on the backup directory (manual restore, merges), suwayomi-server on poco, or syncyomi via sy/komikku fork. undecided
- `bleopt history_share=1` (added 09-21 as the "history not saved on unclean exit" fix) turned out to inflate HISTFILE: 34 MB, 1,000,001 entries, 11,608 unique. terminal took seconds to open. reverted everywhere, `shopt -s histappend` + `PROMPT_COMMAND='history -a'` instead, `HISTFILESIZE` set alongside `HISTSIZE`
  - ble.sh 0.4.0-devel4 `ble/builtin/history/.initialize`: `rskip` is a LINE count (`wc -l`) but gets clamped with `max-min+1`, an ENTRY count. with `HISTTIMEFORMAT` set each entry is 2 lines, so rskip halves and half the file is re-read as new on every shell start, then appended back. compounding across screen windows
  - reproduced with a blerc containing only `history_share=1`: 20 seeded entries -> 32 after two commands. not fzf, not bash-completion -- neither is in that path
  - gotcha while fixing: shells started before the config change keep the old in-memory list and clobber a cleaned HISTFILE on their next prompt. kill them first, then clean
  - [[cheat sheet/ble.sh]]
- comfy-env restructured: config payloads that were byte-identical across scripts moved to `files/` (blerc, bashrc, bash_profile, init.vim, screenrc, atuin.toml, motd.sh, 2x dnf-automatic), installed with `install -D`. scripts 1205 -> 427 lines. no longer a single-file curl target, clone and run
- comfy-env: shared configs moved to `files/` (other session), install line now tarball (no git needed). screenrc: truecolor on, backslash+CR for alt/shift+enter, `C-a h` = hardcopy -h, `slog` in ~/.bashrc strips escapes from screenlogs
- scrubbed specific host addresses from git history of this vault and `comfy-env` (the other 6 public repos had none), `git filter-repo --replace-text` + force-push. real work/lab subnets renumbered to meaningless generic ones, last octet preserved so multi-host notes stay coherent
  - default ranges left alone -- generic, reveal nothing
  - mapping in `~/scrub-lan-addrs.replacements` (local only, never commit it), bundles in `~/repo-backups-20260923/`
  - every commit hash changed: other clones of these two repos need a fresh clone
  - github keeps the pre-rewrite commits reachable by SHA until it gc's; delete+recreate the repo if that matters
- claude code permission gate only sees the literal command text, not what a script contains -- `bash foo.sh --apply` reads as "run some script", so anything destructive inside it is refused no matter the allowlist. makes sense: no way to classify generic code without executing it. issue destructive git as individual commands so the rules can match
  - allowlist entries need the glob: `Bash(git filter-repo:*)`, not `Bash(git filter-repo)`
  - the gate also blocked a journal entry that spelled out the old->new address mapping -- would have re-published the octets the scrub just removed
